# YOLOv7-tiny with RCSOSA modules optimization

# parameters
nc: 80  # number of classes
depth_multiple: 1.0  # model depth multiple
width_multiple: 1.0  # layer channel multiple

# anchors
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

# yolov7-tiny backbone with RCSOSA
backbone:
  # [from, number, module, args]
  [[-1, 1, Conv, [32, 3, 2, None, 1, nn.LeakyReLU(0.1)]],  # 0-P1/2
  
  [-1, 1, Conv, [64, 3, 2, None, 1, nn.LeakyReLU(0.1)]],  # 1-P2/4
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [64]],  # 2
  [-1, 1, Conv, [64, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 3
  
  [-1, 1, MP, []],  # 4-P3/8
  [-1, 1, Conv, [64, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-3, 1, Conv, [64, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-1, 1, Conv, [64, 3, 2, None, 1, nn.LeakyReLU(0.1)]],
  [[-1, -3], 1, Concat, [1]],  # 8
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [128]],  # 9
  [-1, 1, Conv, [128, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 10
  
  [-1, 1, MP, []],  # 11-P4/16
  [-1, 1, Conv, [128, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-3, 1, Conv, [128, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-1, 1, Conv, [128, 3, 2, None, 1, nn.LeakyReLU(0.1)]],
  [[-1, -3], 1, Concat, [1]],  # 15
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [256]],  # 16
  [-1, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 17
  
  [-1, 1, MP, []],  # 18-P5/32
  [-1, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-3, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-1, 1, Conv, [256, 3, 2, None, 1, nn.LeakyReLU(0.1)]],
  [[-1, -3], 1, Concat, [1]],  # 22
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [512]],  # 23
  [-1, 1, Conv, [512, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 24
  ]
# yolov7-tiny head with RCSOSA
head:
  [[-1, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-2, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-1, 1, SP, [5]],
  [-2, 1, SP, [9]],
  [-3, 1, SP, [13]],
  [[-1, -2, -3, -4], 1, Concat, [1]],
  [-1, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [[-1, -7], 1, Concat, [1]],
  [-1, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 32
  
  [-1, 1, Conv, [128, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-1, 1, nn.Upsample, [None, 2, 'nearest']],
  [17, 1, Conv, [128, 1, 1, None, 1, nn.LeakyReLU(0.1)]], # route backbone P4
  [[-1, -2], 1, Concat, [1]],
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [128]],  # 37
  [-1, 1, Conv, [128, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 38
  
  [-1, 1, Conv, [64, 1, 1, None, 1, nn.LeakyReLU(0.1)]],
  [-1, 1, nn.Upsample, [None, 2, 'nearest']],
  [10, 1, Conv, [64, 1, 1, None, 1, nn.LeakyReLU(0.1)]], # route backbone P3
  [[-1, -2], 1, Concat, [1]],
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [64]],  # 43
  [-1, 1, Conv, [64, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 44
  
  [-1, 1, Conv, [128, 3, 2, None, 1, nn.LeakyReLU(0.1)]],
  [[-1, 38], 1, Concat, [1]],
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [128]],  # 47
  [-1, 1, Conv, [128, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 48
  
  [-1, 1, Conv, [256, 3, 2, None, 1, nn.LeakyReLU(0.1)]],
  [[-1, 32], 1, Concat, [1]],
  
  # ELAN-tiny replaced with RCSOSA
  [-1, 1, RCSOSA, [256]],  # 51
  [-1, 1, Conv, [256, 1, 1, None, 1, nn.LeakyReLU(0.1)]],  # 52
  
  [44, 1, RepConv, [128, 3, 1, None, 1, nn.LeakyReLU(0.1)]],
  [48, 1, RepConv, [256, 3, 1, None, 1, nn.LeakyReLU(0.1)]],
  [52, 1, RepConv, [512, 3, 1, None, 1, nn.LeakyReLU(0.1)]],

  [[53,54,55], 1, Detect, [nc, anchors]],   # Detect(P3, P4, P5)
  ]